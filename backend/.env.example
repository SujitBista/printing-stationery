# Backend
NODE_ENV=development
PORT=3001

# PostgreSQL
DATABASE_URL=postgresql://postgres:password@localhost:5432/printing_stationery

# Auth (documented for future use; not required this milestone)
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=7d
